# Normalize line endings to LF in the repo; Git converts to CRLF on checkout on Windows if core.autocrlf=true
* text=auto eol=lf

# Treat certain files as binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.gif binary
*.webp binary

# Keep lockfiles consistent
pnpm-lock.yaml text eol=lf
package-lock.json text eol=lf
